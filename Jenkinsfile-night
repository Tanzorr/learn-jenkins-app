pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
              sh "docker build -t -f ci/Dockerfile-playwrite my-palywright ."
              sh "docker build -t -f ci/Dockerfile-aws-cli my-aws-cli ."
            }
    }

}
